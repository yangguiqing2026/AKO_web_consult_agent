# AKO 网站咨询系统 · 测试验收总表

**文档编号**: AGE-TECH-AKO-WEB-001-TEST
**版本**: v1.0.0
**编制日期**: 2026-07-17
**用法**: VS Code 按 P1→P2→P3 顺序写完代码后，**按本表自上而下逐一打勾**；任一 FAIL 先修再继续。每个 Phase 末尾的回归项必须全过才允许进下一个 Phase。

---

## A. Phase 1 · 骨架验收（13 项）

前置：发布集 `ako_taoli_web_arch` 已灌入 20~30 篇精选语料；`start.bat` 已启动网关 :7863。

| # | 测试项 | 操作 | 通过标准 |
|---|---|---|---|
| A01 | 健康检查 | `curl http://localhost:7863/health` | 200；`doc_count>0`；含 `kb_updated_at`、`embedding_model` |
| A02 | 启动版本锁 | 把 `.env` 的 `embed_model` 改成错误值重启 | 进程拒绝启动并打印不一致原因；改回后正常 |
| A03 | 正常问答 | 问"陶粒墙板的隔音效果如何？" | SSE 逐段流出（非整段）；含 `[1]` 引用 |
| A04 | 来源脱敏 | 同上，看返回 sources | 只有对外显示名；无内部编号/路径/扩展名 |
| A05 | 默认显示名 | 问一篇未进 `source_map.json` 的文档能答的问题 | 来源显示《阿格产品资料》 |
| A06 | 低分兜底 | 问"量子计算机的原理" | `action=lead` 或 refuse 话术；**不调 LLM 硬答** |
| A07 | 商机意图 | 问"1000 平米大概多少钱？" | `action=lead`，返回留资话术 |
| A08 | 闲聊收敛 | 发"你好" | 固定话术；日志 action=chitchat |
| A09 | 留资提交 | 提交 姓名/电话/意向市场 | `{"ok":true}`；`data\leads.jsonl` 新增合法 JSON 行 |
| A10 | 流式中断 | 问答流出一半关闭页面 | 当日日志 `aborted=true`；`/health` 仍 200 |
| A11 | 限流 | 同 IP 1 分钟内连发 11 次 | 第 11 次 429 固定话术 |
| A12 | 敏感词 | 发问含敏感词条的问题 | `action=refuse` 固定话术 |
| A13 | 阈值热调 | POST `/api/admin/threshold` 改 0.7 → 问边缘问题 → 改回 | 免重启生效；`GET` 返回新值；错 token 403 |

**A 组通过标准**：13/13 → 进 Phase 2。

---

## B. Phase 2 · 业务化验收（10 项 + 回归）

前置：A 组全过；`hub_meta.db` 路径已确认（默认 `D:\AKO_Hub\hub_meta.db`）。

| # | 测试项 | 操作 | 通过标准 |
|---|---|---|---|
| B01 | 规则命中 | 问含"报价"的问题 | action=lead；日志 `rule_id=R001` |
| B02 | 规则热加载 | 加一条临时规则（如关键词"测试蜜罐"→lead） | 不重启即生效；测完还原并复原 |
| B03 | 兜底规则保护 | 尝试删 `R900` 后提问 | 启动/加载报错或被拒绝，不产生裸奔 |
| B04 | 留资入库 | 提交留资 → 查 `web_leads` | 新行字段齐全；`leads.jsonl` 同步备份 |
| B05 | DB 故障降级 | 暂时把 `hub_meta_db` 指向只读目录 → 提交留资 | 流程不崩；仅 JSONL + 日志 WARN |
| B06 | 问答脱敏 | 问答后查 `web_qa` | `ip_hash` 非明文；**全表搜不到 A09 提交的电话** |
| B07 | 周报 | `python scripts\analyze_qa.py --days 7` | 落盘 `D:\AKO_Hub\geo_output\`；5 节齐全含未命中清单 |
| B08 | 测试集扩写 | 种子 10 条 → `build_testset.py` | `testset.csv` 40 行；三列正确 |
| B09 | 阈值校准 | 人工校对 CSV 后 `calibrate_threshold.py --apply --token xxx` | 输出 F1 最优阈值+扫描表+错分清单；`GET /api/admin/threshold` 已是新值 |
| B10 | 前端品牌化 | 逐项核对 | 五色值无纯白 `#FFFFFF`；描边 `#231E1C`；AKO 字标；3 个市场快捷按钮；来源折叠；留资成功琥珀金态；更新日期；手机宽度（≤480px）全屏正常 |
| B11 | **回归** | 重跑 A01~A13 | 13/13 仍全过 |

**B 组通过标准**：B01~B10 全过 + B11 回归全过 → 进 Phase 3。

---

## C. Phase 3 · 上线验收（7 脚本项 + 6 人工项 + 回归）

前置：B 组全过；云服务器（2C2G）已就位；域名与证书已申请。

### C-1 脚本项（tests/test_phase3.py）

| # | 测试项 | 通过标准 |
|---|---|---|
| C01 | 发布 dry-run | 输出增删改计数；库零变更 |
| C02 | 发布幂等 | 同 manifest 连跑两次 `--execute`，第二次全 `unchanged` |
| C03 | 发布一致性 | `source_map.json` 与 manifest 一致；metadata 三项（模型版本/更新日期/manifest 版本）齐全 |
| C04 | 镜像同步 | 本地镜像实例收快照后 `/health` 的 `doc_count`、`kb_updated_at` 与源一致；能正常问答 |
| C05 | 原子切换 | 重推同快照：现役版本不变、`_backup` 已生成 |
| C06 | 监控告警 | 健康目标无告警；错端口目标 → `alert.log` 或 webhook 有记录（连续 2 轮才告） |
| C07 | embed.js | `GET /embed.js` 200，内容含云端域名 |

### C-2 人工项（按 deploy/RUNBOOK.md 打勾）

| # | 测试项 | 通过标准 |
|---|---|---|
| C08 | 云端健康 | `curl https://域名/health` 200；返回中无内部库路径（mirror 模式生效） |
| C09 | 数据一致 | 云端 `doc_count` = 本地发布集 `doc_count` |
| C10 | **核心判据** | 本地网关**关机**，官网咨询窗仍正常问答、流式正常 |
| C11 | 应急切 B | 停云端网关 → nginx 切 frp 备用 upstream → 官网恢复应答；切回 C 后正常 |
| C12 | 官网嵌入 | 真实官网页面嵌入代码 → 咨询窗弹出、SSE 逐字流式（验证反代未缓冲） |
| C13 | 告警实触发 | 拔云端服务 → 10~20 分钟内 webhook/alert.log 收到告警 → 恢复后无持续误报 |
| C14 | **回归** | 重跑 A01~A13、B01~B10 | 全部仍过 |

**C 组通过标准**：C01~C13 全过 + C14 回归全过 → **系统正式上线**。

---

## D. 验收后收尾（3 项）

| # | 事项 | 说明 |
|---|---|---|
| D01 | 阈值定稿 | 把 B09 校准出的最优阈值写进 `.env` 固化（热调值重启会丢） |
| D02 | 参数回填白皮书 | 实测值（检索耗时/首字延迟/最优阈值/并发）回填白皮书 v1.1 性能锚定表 |
| D03 | 运维节奏 | 周报每周一跑；manifest 更新随内容部节奏；监控常驻计划任务 |
